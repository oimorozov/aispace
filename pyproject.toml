[project]
name = "aispace"
version = "0.1.0"
description = "A personal workspace for connected AI tasks"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["backend"]
